*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
/.idea/deploymentTargetSelector.xml
.DS_Store
/build
# every module's generated output, not just the root one
build/
/captures
.externalNativeBuild
.cxx
local.properties
cc*
.kotlin/sessions
venv
.venv
backups/
# Deezer ARL credential, never commit
.deezer_arl.local
__pycache__/

# Wiktionary scrape cache for the mots fleches dictionary. Not tracked: it is 12 MB, it is only an
# input to regenerating the shipped asset (which is tracked), and generate_motsfleches.py refetches
# whatever is missing in a couple of minutes.
data/motsfleches_senses.json
