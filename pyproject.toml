[project]
name = "cognitools-dataset"
version = "0.1.0"
description = "Generate the Undercover word pairs and the mots fléchés assets for the CogniTools Android app"
requires-python = ">=3.10"
dependencies = [
    "fasttext",
    "faiss-cpu",
    "numpy",
    "pandas",
]

[dependency-groups]
notebook = ["ipykernel"]

[tool.uv]
package = false

[tool.ruff]
line-length = 110

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
